{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "check": { "cache": false },
    "fix": { "cache": false },
    "test": { "cache": false }
  }
}
